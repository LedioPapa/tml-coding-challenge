spring:
  profiles:
    active: era
  cache:
    cache-names: exchangeRatesCache
    caffeine:
      spec: expireAfterWrite=10s

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

debug: false
